version: "2.2"

services:
 cineweb-user-api:
  container_name: cineweb-user-api
  image: cineweb-user-api
  environment:
  - PORT=5000
  ports:
  - 5000:5000
  build:
   context: .
   dockerfile: Dockerfile
